{
  "name": "@convergio/create-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CLI tool to scaffold Convergio starter templates into a project tree",
  "main": "./dist/index.js",
  "types": "./dist/types/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
