name: Deploy Pages

on:
  push:
    branches: [main]

permissions:
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci && npm run build

      - name: Prepare Pages site
        run: |
          mkdir -p _site/dist _site/src
          cp -r dist/* _site/dist/
          cp -r src/css _site/src/css
          cp -r src/fonts _site/src/fonts
          cp -r src/assets _site/src/assets
          cp -r src/wc _site/src/wc
          cp demo/index.html _site/index.html
          cp -r demo/sections _site/sections
          cp demo/app.js _site/app.js
          sed -i 's|href="../|href="|g' _site/index.html
          sed -i 's|src="../|src="|g' _site/index.html
          sed -i 's|src="app.js"|src="app.js"|g' _site/index.html

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v5
